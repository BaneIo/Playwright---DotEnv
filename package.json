{
  "name": "playwright-dotenv",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test automation project using dotenv, Allure and GitHub Actions",
  "scripts": {
    "clean": "if exist allure-results (rmdir /s /q allure-results) && if exist allure-report (rmdir /s /q allure-report)",
    "test": "npx playwright test",
    "allure:generate": "npx allure generate allure-results --clean -o allure-report",
    "allure:open": "npx allure open allure-report",
    "allure:report": "npm run clean && npm run test && npm run allure:generate && npm run allure:open"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "allure-commandline": "^2.36.0",
    "allure-playwright": "^3.0.0"
  }
}
